/**
 * GENERATED FILE — do not edit by hand.
 *
 * Produced by `pnpm --filter @repo/api-client generate` (openapi-typescript) from the backend
 * OpenAPI document at /api/v1/openapi.json. This is a placeholder committed so the package builds
 * before the backend exists; running `generate` overwrites it with the real types.
 */

export interface paths {}

export interface components {
  schemas: Record<string, never>;
}

export interface operations {}

export type webhooks = Record<string, never>;
