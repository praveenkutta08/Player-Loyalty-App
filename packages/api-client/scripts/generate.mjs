// Placeholder generator for the typed API client.
//
// In a later phase this script fetches the backend OpenAPI document
// (http://localhost:8000/api/v1/openapi.json) and generates the typed client + RTK Query
// endpoints into `src/`. Until the backend exists, it is a no-op that explains itself.
console.log(
  '[api-client] generate: backend OpenAPI not wired yet. ' +
    'This will generate src/ from /api/v1/openapi.json once the backend is scaffolded.',
);
