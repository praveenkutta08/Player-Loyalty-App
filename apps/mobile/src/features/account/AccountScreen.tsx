import React from 'react';

import { ComingSoon } from '../../components/ComingSoon';

/** Account tab (C1). Points, tier, activity & profile land in P4.5. */
export function AccountScreen(): React.JSX.Element {
  return <ComingSoon title="Account" subtitle="Loyalty, tier & profile land in P4.5." />;
}
