import React from 'react';

import { ComingSoon } from '../../components/ComingSoon';

/** Home tab (H1). Brandable hero + tiles arrive in P4.4. */
export function HomeScreen(): React.JSX.Element {
  return <ComingSoon title="Home" subtitle="Your personalized home lands in P4.4." />;
}
