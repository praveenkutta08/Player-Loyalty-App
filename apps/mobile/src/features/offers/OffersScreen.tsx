import React from 'react';

import { ComingSoon } from '../../components/ComingSoon';

/** Offers tab (O1/O3, segmented Offers | Promotions | My Rewards). Content lands in P4.4. */
export function OffersScreen(): React.JSX.Element {
  return <ComingSoon title="Offers" subtitle="Targeted offers & promotions land in P4.4." />;
}
